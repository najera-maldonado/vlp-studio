version: '3.8'

services:
  nanocapsule:
    build:
      context: .
      dockerfile: Dockerfile
    image: nanocapsule-mvp:latest
    container_name: nanocapsule-designer
    ports:
      - "5000:5000"
    volumes:
      # Mount input/output directories for persistence
      - ./Input:/app/Input
      - ./Output:/app/Output
      - ./config:/app/config
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=0
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - nanocapsule-net
    healthcheck:
      # python (stdlib) en vez de curl, que no está en la imagen slim.
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:5000/api/health').status==200 else 1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add nginx for production deployment
  # nginx:
  #   image: nginx:alpine
  #   container_name: nanocapsule-nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - nanocapsule
  #   networks:
  #     - nanocapsule-net

networks:
  nanocapsule-net:
    driver: bridge

# Volumes for persistent data (optional)
volumes:
  input_data:
  output_data: