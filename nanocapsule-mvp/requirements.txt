# Dependencias de Python — VLP Studio
# Pines alineados con el entorno que REALMENTE funciona (Python 3.12), no con los
# fósiles anteriores (que fijaban Flask 2.3.2 y NO declaraban RDKit → irreproducible).
# Actualizado en VLP-01 (2026-09-17). Ver ESTADO.md §4.
#
# Este archivo son las deps DIRECTAS (legible por humanos). Para reproducir el entorno
# EXACTO (incl. transitivas pinneadas) usa `requirements.lock` (generado con pip-compile):
#   pip install -r requirements.lock
# Regenerar el lock tras cambiar algo aquí:  pip-compile --strip-extras requirements.txt

# --- Núcleo usado por el código (src/) ---
Flask==3.1.3
Werkzeug==3.1.8
PyYAML==6.0.1
numpy==1.26.4
rdkit==2025.9.1          # geometría 3D de sustratos por SMILES (STUDIO 3D + PAC-PORE)

# --- Motores externos (NO son paquetes pip; instalar en el sistema) ---
# El código los invoca por subprocess. Sin ellos, las puertas que los usan fallan.
#   packmol            (apt: packmol)            · empaquetado
#   pymol              (apt: pymol / conda)      · radio interno, cribado de mutantes
#   openbabel → obabel (apt: openbabel)          · preparación de receptor/ligando
#   autodock-vina → vina (apt: autodock-vina)    · docking
#   hole               (NO en apt — Oxford, manual) · perfil del poro
#   idock              (NO en apt — manual)         · docking alternativo (Poromania)

# --- Desarrollo (opcional) ---
pytest==7.4.0
pytest-cov==4.1.0

# --- Declaradas antes pero NO importadas en src/ (revisar/retirar en VLP-05) ---
# biopython, scipy, pandas, flask-cors, python-dotenv, click, tqdm, requests
